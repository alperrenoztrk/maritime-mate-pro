<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:allowBackup="false"
        android:networkSecurityConfig="@xml/network_security_config"
        android:hardwareAccelerated="true"
        android:largeHeap="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|locale|smallestScreenSize|screenLayout|uiMode|navigation"
            android:name=".MainActivity"
            android:label="@string/title_activity_main"
            android:theme="@style/AppTheme.NoActionBarLaunch"
            android:launchMode="singleTask"
            android:exported="true">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <!-- Supabase Google girişi, OAuth turunu bu özel şema ile
                 uygulamaya geri döndürür (com.marinersbook.app://auth/callback).
                 Aynı adresin Supabase → Authentication → URL Configuration →
                 Redirect URLs listesinde de bulunması gerekir. -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="@string/custom_url_scheme" />
            </intent-filter>

        </activity>

        <!-- AdMob: @capacitor-community/admob eklentisi için zorunlu; eksik
             bırakılırsa uygulama açılışta çöker. Değer build.gradle'da
             çözülür: android/admob.properties → ADMOB_APP_ID_ANDROID ortam
             değişkeni → Google'ın resmi TEST App ID'si. -->
        <meta-data
            android:name="com.google.android.gms.ads.APPLICATION_ID"
            android:value="${admobAppId}" />

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths"></meta-data>
        </provider>
    </application>

    <!-- Permissions -->

    <uses-permission android:name="android.permission.INTERNET" />
    <!-- Çevrimdışı durum tespiti -->
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <!-- POST_NOTIFICATIONS bilerek yok: belge hatırlatmaları Web Notification
         API'si ile gösteriliyor (src/components/documents/DocumentExpiryNotifier.tsx)
         ve Android WebView bu API'yi uygulamıyor — izin native tarafta hiçbir
         zaman kullanılmıyordu. PWA/web sürümünde bildirimler tarayıcı iznine
         bağlı olarak çalışmaya devam eder. Native bildirim eklenirse
         (@capacitor/local-notifications vb.) izin geri eklenmelidir. -->
    <!-- Canlı konum takibi (WebView geolocation için gerekli) -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <!-- GPS donanımı zorunlu değil; konum yoksa uygulama yine çalışır -->
    <uses-feature android:name="android.hardware.location.gps" android:required="false" />
</manifest>
