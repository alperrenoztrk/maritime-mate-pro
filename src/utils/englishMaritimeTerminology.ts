// Canonical implementation lives beside the Supabase edge functions so the
// browser, build scripts and server-side translator use one rule set.
export * from '../../supabase/functions/_shared/englishMaritimeTerminology.ts';
